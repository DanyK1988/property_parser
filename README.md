# ✅README файл для скраперов сайтов недвижимости.  
================================================  
## 🌏Описание проекта. 
Данный проект производит скрапинг с популярных сайтов недвижимости. Реализованы 2 разные технологи:  
 1️⃣ Скрапинг с помощью библиотеки Scrapy - файл properties.py  
 2️⃣ Срапинг динамических сайтов с помощью библиотеки Selenium и Scrapy  unicorn_property.py
   
## 🏋🏻‍♀️ Описание работы парсеров  
### Property.py  
Данный парсер работает со статичным сайтом крупного агентства недвжимости Intermark global. В работе используется только библиотека Scrapy. Весь сайт состоит из карточек объектов недвижимости по которым можно перейти с детальной информацией на отдельно странице. Это упрощает сбор информации. Парсер настроен на страницу с недвижимостью индонезии, но это легко изменить на любой другой раздел. Что происходит под капотом? Паук попадает на страницу через методо start_requests. Открывается начальная страница и передается методу parse. Этот метод находит карточки на странице и передает кажду из них в метод parse_item. Если карточек больше не осталось, то происходит открытие следующей страницы и рекурсивно вызается метод parse. Он собирает и очищает всю необходимую информацию (Название, стоимость, описание и тд) в словарь и передает на запись в БД.  
Структура БД описана в файле pipelines.py, а подключение к БД реализовано в файле db.py. Файл settings.py описывает настройки парсера, по умолчанию LOG_LEVEL настроен только на INFO, тк парсер шлет много технической информации, сам процесс детально залогирован и техническая информация излишня.
Для записи информации в БД необходимо настроить файл .env
Скрапер запускается по команде scrapy crawl property.py
По завершении скрапинга и записи данных в БД нужно запустить файл analytic.py. Данный скрипт запускает обработку данных через PySpark и записывает обработанные данные в витрину данных. 

### Unicorn_property.py
Данный скрапер работает с динамическим сайтом unicorn-property.com. Проект разработывался больше в учебных целях и изучения библиотеки Selenium. Процесс скрапинга состоит из следующих шагов: Selenium открывает браузер, анализирует страницу и находит карточки недвижимостей. Проблема в самой структуре сайта, клики на карточки скрываются за JS скриптом, поэтому было не просто разобраться как нажать на них. После нажатия карточка передается в Scrapy, который уже вытягивает всю информацию и логирует в консоль. Основная задача заключалась в изучении библиотеки Selenium, поэтому сейчас не происходит запись информации в БД, плюс информацию структурирована иначе чем в property.py. Скрапер запускается командой scrapy crawl unicorn_property.py

## Следующие шаги
1. Автоматический запуск скрпита analytic.py по завершении скрапинга
2. Структурировать информацию в скрапере unicorn_property.py 
3. Запись информации unicorn_property.py в БД 